# AGENTS.md

## Cursor Cloud specific instructions

This repository currently contains no source code, build systems, dependencies, or runnable services. It has a single file (`ПР.txt`) with placeholder Cyrillic text.

- **No package manager, language runtime, or dependency installation is needed.**
- **No lint, test, build, or run commands are available.**
- If code is added in the future, update this section with the relevant dev setup, lint/test/build/run commands, and any non-obvious caveats.
